@model Product
<p>
    <b>@Model.ProductName</b> isimli ürünü silmek istediğinizden emin misiniz?
    <form asp-action="Delete" method="post">
        <input type="hidden"value="@Model.ProductId" name="id"> 
        <button type="submit" class="btn btn-danger">Evet</button>
        <a asp-action="Index" class="btn btn-primary">Hayır</a>
    </form>
</p>